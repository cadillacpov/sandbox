import q3_random_demo

# Define user range
min_val = 1
max_val = 100

# Call the function from the module
result = q3_random_demo.get_random_in_range(min_val, max_val)

print(f"Your random number between {min_val} and {max_val} is: {result}")